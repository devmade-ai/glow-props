# Session Notes

**`## Communication` is a goal, not rules — and it is on trial.**

Replaced 2026-08-17: twelve bullets and a prescribed three-part reply shape
became one goal and five tests. The other 18 fleet repos still carry the old
bullets.

- **That split is deliberate.** An `align` pass will read this repo as drifted.
  It has not drifted — the fleet is behind. Do not level gp-props back down to
  the twelve bullets, and do not propagate the goal until the user says so.
- **No script can verify it.** It is checked by noticing which of the five tests
  a reply failed. Two misses so far, both caught by the user: a boilerplate
  "say the word for X" appended to replies with nothing behind it (the
  `Cheap to read` wording invited it, now reworded), and a fix reported as
  committed that had never been applied.

Delete this once the goal has held for a few sessions and the fleet is synced.
